()_ f(){

    $_ x;
    ? ( ( true && true ) || ( ( x > 0  || ( x < 10 ) && ( x< 10) ) ) ) {
        # x <- 1;
        # ?( x = 2 ){
        #     x <- 3;
        #     ?( x = 4 ){
        #         break;
        #         break;
        #     }
        #     : () {
        #         x <- 5;
        #         break;
        #         break;
        #     }
            
        # }
        # x <- 4;
    }
    # x <- 1 + (3 + (5 * 4 * (4 * 4 * ( 2 + 1))));
    # $_y;
    # loop(true){
    #     x <- x * 1;
    #     loop(true){
    #         loop()
    #     }
    #     x <- x + 1;
    # }
    # x <- x + 3;
    # $_ y;
    # loop(true){
    #     ? ( ( (true && ( false && false )) ) ){
    #         x <- x * 3;
    #         break;
    #     }
    #     break;
    # }

}

