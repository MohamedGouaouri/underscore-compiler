# from asyncore import loop


()_ f(){

    $_ x;
    # loop ( x <- 0; x < 10; x <- x + 2 ) {
        ?( x > 0 && ( x = 9 || x > 9 ) && true ){
            x <- 1;
            # break;
        } : (){
            x <- x * 3;
        }
        # continue;
    # }
    # $_ y;
    # loop(true){
    #     ? ( ( (true && ( false && false )) ) ){
    #         x <- x * 3;
    #         break;
    #     }
    #     break;
    # }

}

