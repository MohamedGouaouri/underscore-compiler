()_ f(){

    $_ x;
    # loop ( x <- 0; x < 10; x <- x + 2 ) {
        ?( true && true && x = 9  || ( x = 9 ) || (x > 10)  || x < 10 ){
            x <- 1;
            # break;
        }
        # continue;
    # }
    # $_ y;
    # loop(true){
    #     ? ( ( (true && ( false && false )) ) ){
    #         x <- x * 3;
    #         break;
    #     }
    #     break;
    # }

}

